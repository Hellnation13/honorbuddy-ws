JSM.module("Model", function(Model, JSM, Backbone, Marionette, $, _) {

	Model.Screenshot = Backbone.Model.extend({
		
	});
	Model.ScreenshotCollection = Backbone.Collection.extend({
		model: Model.Screenshot
	});
	
	
	Model.Stats = Backbone.Model.extend({
		
	});
	
});